---
phase: 01-copy-sweep-foundation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
- src/web/locales/en-US.json
- src/web/setup.html
- src/api.rs
- src/settings.rs
- scripts/render_check.js
- tests/fixtures/locales/public-en-US.json
autonomous: true
requirements:
- COPY-01
- COPY-02
- COPY-03
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 3
  confidence: med
must_haves:
  truths:
  - Setup wizard steps 1-4 read provider-neutral (generic API key wording, Add API keys stepper, 40-rpm hint and nvapi- placeholder intact)
  - Dashboard and settings show no user-visible NIM-specific string contradicting multi-provider reality
  - check_i18n and locale_v1 --all pass with fresh sha256(en)[:8] hashes and regenerated public fixture
  - en-XA pseudolocale renders the edited surfaces
  artifacts:
  - src/web/locales/en-US.json (reworded en/desc, recomputed hashes, ids frozen)
  - tests/fixtures/locales/public-en-US.json (regenerated atomically with catalog)
  - src/web/setup.html (fallback nodes reworded, data-i18n keys untouched)
  - src/api.rs and src/settings.rs (user-visible doc rewords only)
  key_links:
  - catalog en values to sha256(en)[:8] hashes (check_i18n.py:1442 enforces)
  - catalog to public fixture via locale_v1.py --update-public (lands atomically, never stale)
  - catalog ids to DOM via data-i18n keys (ids frozen, values only)
---

<objective>
Verify-first audit and gap-closure of the already-implemented copy sweep: confirm every user-visible NIM-specific string is provider-neutral per the locked decisions, close any residual gaps inside COPY-01/02/03 scope only, and land catalog + fixtures + i18n gates green. Per D-coverage: implements all CONTEXT.md locked decisions (wizard/dasboard wording, sweep mechanics) and UI-SPEC copywriting + i18n-mechanics contracts.

Purpose: The sweep exists uncommitted in the tree (29 catalog en + 27 desc reworded, 4 setup.html fallbacks, 7 Rust doc rewords, 3 render_check literals, regenerated fixtures). Blind redo would churn and risk the mixed multi-upstream tree. This plan audits first, edits only residuals.
Output: Gap-closed copy sweep with green i18n gates, ready for the plan-02 proof stack.
</objective>

<execution_context>
@/home/shivam/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shivam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-copy-sweep-foundation/01-CONTEXT.md
@.planning/phases/01-copy-sweep-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Audit sweep end-to-end — one surface per layer</name>
  <files>src/web/locales/en-US.json, src/web/setup.html, src/api.rs, scripts/render_check.js</files>
  <action>Trace ONE edited string per layer and prove the full chain holds: pick wizard step-2 hint catalog entry, confirm its data-i18n key renders in src/web/setup.html fallback and setup.js catalogMessage path, confirm its hash equals sha256(en)[:8] recomputed by hand, and confirm the public fixture carries the same value. Then widen to a full residual scan: every remaining NIM/Nvidia/nvidia token in user-visible scope must classify as (a) frozen catalog id such as settings.users.nim_key_count.* or settings.validation.nim_key_required, (b) data-pass-through such as shared.js publisher entries or model ids, (c) internal-only such as NIMPROXY_* env vars, comments, or test descriptions in render_check.js asserted literals, or (d) a genuine residual requiring a reword per CONTEXT.md locked decisions D-wizard/D-dashboard (generic API key noun, Add API keys stepper, nvapi- placeholder kept, 40-rpm hint kept, provider wording for governor, {n} API keys plurals across all six CLDR forms) and UI-SPEC voice rules (provider-neutral always, placeholders sacred, sentence case, no new em-dash, existing em-dashes untouched). Reword genuine residuals only; never rename catalog ids, DOM ids, data-* attributes, CSS classes, routes, metric names, or config keys; never touch docs or screenshots (out of scope per D-sweep-mechanics).</action>
  <verify>
    <automated>rg -n "NIM|Nvidia|NVIDIA" src/web/setup.html src/web/dashboard.html src/web/login.html src/web/dashboard.js src/web/settings.js src/web/setup.js | grep -v -i "nimproxy_" ; python3 -c "import json,hashlib; c=json.load(open('src/web/locales/en-US.json')); ms=c['messages']; bad=[k for k,v in ms.items() if v.get('hash')!=hashlib.sha256(v.get('en','').encode()).hexdigest()[:8]]; print('stale-hashes:', bad if bad else 'NONE')"</automated>
  </verify>
  <done>One string per layer traced end-to-end; every residual NIM token classified a/b/c/d; genuine residuals reworded with placeholders and CLDR sets intact; zero identifier renames.</done>
</task>

<task type="auto">
  <name>Close desc + Rust-doc + asserted-literal gaps</name>
  <files>src/web/locales/en-US.json, src/api.rs, src/settings.rs, scripts/render_check.js</files>
  <action>Close only what the audit flagged, staying inside COPY-01/02/03: update desc fields where they name NIM to provider-neutral translator instructions with placeholder and CLDR notes intact per UI-SPEC section 7 (target the known families: saturation-bar sub-labels, column-header note, plural-category notes); reword OpenAPI-visible Rust docs in src/api.rs and src/settings.rs ONLY where user-visible and NIM-naming, preserving meaning and leaving bodies, status codes, content types, metric names, and identifiers untouched per the scope guard; update asserted display literals in scripts/render_check.js ONLY where they assert user-visible copy (the 3 known literals), leaving test descriptions, NIMPROXY_* env vars, and internal comments alone. Do NOT touch settings.dialog.remove_group or any endpoint-group/multi-upstream string — that is Phase 3 scope leaking in the mixed tree, out of bounds for this phase. Do NOT re-litigate openapi.json drift (stale nim-proxy title plus unrecorded models/upstreams endpoints flushed by the earlier regen): acknowledge as pre-existing drift already flushed, verify machine contracts unchanged.</action>
  <verify>
    <automated>rg -n -i "nim" src/web/locales/en-US.json | grep -v "nim_key_count\|nim_key_required" ; rg -n "NIM API|NIM key|Nvidia|NVIDIA" src/api.rs src/settings.rs | grep -v "^\s*//" | head -20</automated>
  </verify>
  <done>All desc fields provider-neutral with placeholder notes intact; Rust docs reworded user-visible-only; asserted literals match new copy; no Phase 3 strings touched; machine contracts unchanged.</done>
</task>

<task type="auto">
  <name>Refresh hashes, regen fixtures, green the i18n gates</name>
  <files>src/web/locales/en-US.json, tests/fixtures/locales/public-en-US.json</files>
  <action>For every edited en value recompute sha256(en)[:8] (never hand-write hashes); regenerate tests/fixtures/locales/public-en-US.json via scripts/locale_v1.py --update-public in the SAME change as the catalog edit so no intermediate stale-hash state exists, per UI-SPEC mechanics contract steps 2-3. Then run the COPY-03 gate set: scripts/check_i18n.py plus its selftest, locale_v1 --all plus its selftest, and gen_pseudolocale --check including an en-XA render of the edited wizard/dashboard surfaces per the locked D-pseudolocale decision. Failures are fixed in this task (hash, placeholder, CLDR-unit, or no-new-em-dash class) — do not advance to plan 02 with red i18n gates.</action>
  <verify>
    <automated>python3 scripts/check_i18n.py && python3 scripts/locale_v1.py --all && python3 scripts/gen_pseudolocale.py --check</automated>
  </verify>
  <done>Every edited en carries a fresh sha256(en)[:8]; public fixture regenerated atomically; check_i18n, locale_v1 --all, and pseudolocale check all green including selftests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator→catalog | desc/en values are untrusted input to the hash + fixture pipeline |
| catalog→DOM | catalog text crosses into HTML only via catalogMessage/data-i18n helpers and the single setup.html fallback boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | en-US.json hashes | medium | mitigate | Recompute sha256(en)[:8] for every edit; check_i18n gate enforces (plan-01 task 3) |
| T-01-02 | Information Disclosure | nvapi-{last4} literals | low | accept | Last-4-only display is the established pattern; no full-key copy introduced |
| T-01-03 | Tampering | setup.html fallback nodes | low | mitigate | Fallback nodes keep data-i18n keys; catalogMessage remains the sink owner per AGENTS.md invariant 2 |
| T-01-SC | Tampering | package installs | high | mitigate | No package installs in this phase; no legitimacy gate triggered |
</threat_model>

<verification>
check_i18n (+selftest), locale_v1 --all (+selftest), gen_pseudolocale --check green; residual-NIM scan classifies clean; catalog ids byte-identical to pre-phase set.
</verification>

<success_criteria>
COPY-01 wizard copy provider-neutral; COPY-02 dashboard/settings copy provider-neutral; COPY-03 hashes fresh, fixtures regenerated, i18n gates green, no new em-dashes.
</success_criteria>

<output>
Create `.planning/phases/01-copy-sweep-foundation/01-01-SUMMARY.md` when done
</output>
