---
phase: 01-copy-sweep-foundation
plan: '02'
type: execute
wave: 2
depends_on:
- 01-01
files_modified:
- openapi.json
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
autonomous: true
requirements:
- COPY-03
estimate:
  tokens: 22000
  raw_tokens: 11000
  tasks: 2
  confidence: med
must_haves:
  truths:
  - Full proof stack green on the swept tree (cargo lib + e2e + openapi, render_check setup page, fmt)
  - Scope guards hold: identifiers frozen, machine contracts unchanged, no disturbance to non-copy work in the mixed tree
  - Phase 1 recorded complete with COPY-01/02/03 traceable
  artifacts:
  - openapi.json (regenerated, drift acknowledged, contracts unchanged)
  - .planning/phases/01-copy-sweep-foundation/01-02-SUMMARY.md
  - REQUIREMENTS.md + ROADMAP.md Phase 1 checkboxes updated
  key_links:
  - swept catalog to passing suites (228 lib + 124 e2e + 8 openapi + render_check + fmt)
  - openapi.json regen to flushed pre-existing drift (stale title, models/upstreams endpoints) — acknowledged, not re-litigated
  - mixed-tree discipline: copy files verified, all other uncommitted work untouched
---

<objective>
Run the full Phase 1 proof stack over the swept tree, enforce scope guards (frozen identifiers, untouched machine contracts, mixed-tree discipline), and record Phase 1 complete. Implements COPY-03 proof half; closes the phase per ROADMAP success criteria (provider-neutral screens, green i18n gates).

Purpose: Plan 01 proves copy correctness; this plan proves nothing else broke and the phase is shippable without disturbing the other pre-existing uncommitted multi-upstream work.
Output: Green proof record, scope-guard evidence, updated REQUIREMENTS/ROADMAP Phase 1 state.
</objective>

<execution_context>
@/home/shivam/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/shivam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-copy-sweep-foundation/01-CONTEXT.md
@.planning/phases/01-copy-sweep-foundation/01-UI-SPEC.md
@.planning/phases/01-copy-sweep-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Full proof stack on the swept tree</name>
  <files>openapi.json</files>
  <action>Rerun the established proof stack from a clean state on the swept tree and fix only copy-caused failures: cargo test covering the lib suite (228), e2e suite (124), and openapi suite (8); node scripts/render_check.js --page setup plus the escape-probe and syntax-only variants; cargo fmt --check. If openapi.json shows diffs beyond the copy rewords, classify them against the KNOWN flushed drift only (stale nim-proxy title, previously unrecorded models/upstreams endpoints from working-tree code that predates this phase): matching known drift is accepted and recorded, anything else is investigated as a contract move and reverted. Regenerate openapi.json if and only if a copy reword changes a description field; never hand-edit it. Do NOT stage or commit anything in this task; do NOT touch non-copy files to make suites pass — a failure rooted in non-copy working-tree code is reported as a finding, not fixed here.</action>
  <verify>
    <automated>cargo test 2>&1 | tail -5; node scripts/render_check.js --page setup 2>&1 | tail -5; cargo fmt --check && echo FMT-OK</automated>
  </verify>
  <done>Cargo lib+e2e+openapi suites green, render_check setup page green with escape-probe and syntax-only variants, fmt clean; openapi.json diffs limited to copy rewords plus acknowledged known drift.</done>
</task>

<task type="auto">
  <name>Scope guards and phase close-out</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Enforce the four scope guards and record the outcome: (1) identifiers frozen — catalog ids including nim_key_count and nim_key_required plus routes, metric names and label values, DOM ids, data-* attributes, CSS classes, sort keys, and config keys byte-identical to pre-phase; (2) machine contracts unchanged — API bodies, status codes, content types, OpenAPI shapes beyond descriptions, config and history formats all intact; (3) string-length discipline — edited strings fit UI-SPEC budgets (stepper 24ch, headers 20ch, buttons 28ch, hints 160ch) with zero CSS changes; (4) mixed-tree discipline — git status shows no NEW modifications outside the copy file set (en-US.json, setup.html fallbacks, api.rs/settings.rs docs, render_check asserted literals, public fixture, openapi.json regen) caused by this phase; the pre-existing multi-upstream work is untouched. Then close out: check COPY-01/02/03 in REQUIREMENTS.md traceability, mark Phase 1 plans in ROADMAP.md, and write the 01-02 SUMMARY with the proof record, guard evidence, the openapi-drift acknowledgement, and explicit pointer to what was left for later phases (theme, multi-group wizard incl. remove_group, screenshots, image rebuild).</action>
  <verify>
    <automated>git status --porcelain | wc -l; git diff --name-only | grep -v -e "src/web/locales/en-US.json" -e "src/web/setup.html" -e "src/api.rs" -e "src/settings.rs" -e "scripts/render_check.js" -e "tests/fixtures/locales/public-en-US.json" -e "openapi.json" -e ".planning/" | head -20</automated>
  </verify>
  <done>All four guards evidenced; REQUIREMENTS COPY-01/02/03 checked with Phase 1 traceability; ROADMAP Phase 1 marked; SUMMARY records proof, guards, drift acknowledgement, and deferred items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| swept-tree→test-suites | proof stack runs against a mixed tree; non-copy code is untrusted input to pass/fail attribution |
| regen→openapi.json | generator output crosses into a committed contract file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | openapi.json regen | medium | mitigate | Regen only from generator on description change; diff classified against known drift; contract moves reverted (task 1) |
| T-01-05 | Denial of Service | mixed-tree attribution | low | mitigate | Non-copy failures reported as findings, never fixed by touching non-copy files (task 1) |
| T-01-SC | Tampering | package installs | high | mitigate | No package installs in this phase; no legitimacy gate triggered |
</threat_model>

<verification>
cargo test (lib+e2e+openapi) green; render_check setup + escape-probe + syntax-only green; fmt clean; i18n gates reconfirmed green; four scope guards evidenced.
</verification>

<success_criteria>
ROADMAP Phase 1 success criteria all TRUE: wizard provider-neutral, dashboard/settings provider-neutral, check_i18n + locale_v1 green with fresh hashes and regenerated fixtures.
</success_criteria>

<output>
Create `.planning/phases/01-copy-sweep-foundation/01-02-SUMMARY.md` when done
</output>
